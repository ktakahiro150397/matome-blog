[project]
name = "sumamrizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain",
    "openai",
    "transformers",
    "whisper",
    "torch",
    "python-dotenv"
]
